name: CD

on:
    workflow_run:
        workflows:
            - CI

        types:
            - completed

        branches:
            - main

permissions:
    contents: read
    packages: write

jobs:
    # ==========================================
    # Deploy
    # ==========================================

    deploy:
        name: Build and Deploy to Linux VPS

        if: ${{ github.event.workflow_run.conclusion == 'success' }}

        runs-on: ubuntu-latest

        steps:
            # ==========================================
            # Checkout exact commit from CI
            # ==========================================

            - name: Checkout
              uses: actions/checkout@v4
              with:
                  ref: ${{ github.event.workflow_run.head_sha }}

            # ==========================================
            # Login to GHCR
            # ==========================================

            - name: Login to GHCR
              uses: docker/login-action@v3
              with:
                  registry: ghcr.io
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            # ==========================================
            # Setup Docker Buildx
            # ==========================================

            - name: Setup Docker Buildx
              uses: docker/setup-buildx-action@v3

            # ==========================================
            # Build + Push Backend
            # ==========================================

            - name: Build and push backend
              uses: docker/build-push-action@v6
              with:
                  context: ./backend
                  file: ./backend/Dockerfile.prod

                  push: true

                  tags: |
                      ghcr.io/${{ github.repository_owner }}/my-resume-backend:sha-${{ github.event.workflow_run.head_sha }}

                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            # ==========================================
            # Build + Push Frontend
            # ==========================================

            - name: Build and push frontend
              uses: docker/build-push-action@v6
              with:
                  context: ./frontend
                  file: ./frontend/Dockerfile

                  push: true

                  build-args: |
                      BUILD_CONFIGURATION=production

                  tags: |
                      ghcr.io/${{ github.repository_owner }}/my-resume-frontend:sha-${{ github.event.workflow_run.head_sha }}

                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            # ==========================================
            # Deploy to Linux VPS
            # ==========================================

            - name: Deploy to Linux VPS
              uses: appleboy/ssh-action@v1.2.2

              env:
                  IMAGE_TAG: sha-${{ github.event.workflow_run.head_sha }}

              with:
                  host: ${{ secrets.VPS_HOST }}
                  username: ${{ secrets.VPS_USERNAME }}
                  key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}

                  envs: IMAGE_TAG

                  script: |
                      set -e

                      echo "=========================================="
                      echo "Deploying to Linux VPS"
                      echo "=========================================="

                      echo "IMAGE_TAG=${IMAGE_TAG}"

                      # ==========================================
                      # Go to application directory
                      # ==========================================

                      cd "${{ secrets.VPS_APP_PATH }}"

                      echo "Application path:"
                      pwd

                      # ==========================================
                      # Create deployment environment
                      # ==========================================

                      echo "Creating .env.deploy"

                      cat > .env.deploy <<EOF
                      IMAGE_TAG=${IMAGE_TAG}
                      EOF

                      # ==========================================
                      # Pull Docker images
                      # ==========================================

                      echo "Pulling Docker images..."

                      docker compose \
                        --env-file .env.prod \
                        --env-file .env.deploy \
                        -f docker-compose.prod.yml \
                        pull

                      # ==========================================
                      # Start / Update containers
                      # ==========================================

                      echo "Starting containers..."

                      docker compose \
                        --env-file .env.prod \
                        --env-file .env.deploy \
                        -f docker-compose.prod.yml \
                        up -d

                      # ==========================================
                      # Show container status
                      # ==========================================

                      echo "Container status:"

                      docker compose \
                        --env-file .env.prod \
                        --env-file .env.deploy \
                        -f docker-compose.prod.yml \
                        ps

                      # ==========================================
                      # Cleanup unused Docker images
                      # ==========================================

                      echo "Cleaning unused Docker images..."

                      docker image prune -f

                      echo "=========================================="
                      echo "Deployment completed"
                      echo "=========================================="
